# Print structs with linebreaks like lldb rather than all on one line
set print pretty on

# Save command history between sessions and keep it global rather than
# per project
set history save on
set history filename ~/.gdb_history

set auto-load safe-path /

set confirm off
